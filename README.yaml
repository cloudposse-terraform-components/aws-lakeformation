name: "aws-lakeformation"
# Canonical GitHub repo
github_repo: "cloudposse-terraform-components/aws-lakeformation"
# Short description of this project
description: |-
  This component is responsible for provisioning Amazon Lake Formation resources.
# Usage examples and guidance
usage: |-
  **Stack Level**: Regional

  Here are some example snippets for how to use this component:

  `stacks/catalog/lakeformation/defaults.yaml` file (base component for all lakeformation deployments with default
  settings):

  ```yaml
  components:
    terraform:
      lakeformation/defaults:
        metadata:
          type: abstract
        settings:
          spacelift:
            workspace_enabled: true
        vars:
          enabled: true
          tags:
            Team: sre
            Service: lakeformation
  ```

  ```yaml
  import:
    - catalog/lakeformation/defaults

  components:
    terraform:
      lakeformation-example:
        metadata:
          component: lakeformation
          inherits:
            - lakeformation/defaults
        vars:
          enabled: true
          name: lakeformation-example
          s3_bucket_arn: arn:aws:s3:::some-test-bucket
          create_service_linked_role: true
          admin_arn_list:
            - arn:aws:iam::012345678912:role/my-admin-role
          lf_tags:
            left: ["test1", "test2"]
            right: ["test3", "test4"]
          resources:
            database:
              name: example_db_1
              tags:
                left: test1
  ```

  <!-- prettier-ignore-start -->
  <!-- prettier-ignore-end -->
# External links and documentation
references:
  - name: cloudposse-terraform-components
    description: "Cloud Posse's upstream component"
    url: https://github.com/orgs/cloudposse-terraform-components/repositories
tags:
  - component/lakeformation
  - layer/unassigned
  - provider/aws
# Categories of this project
categories:
  - component/lakeformation
  - layer/unassigned
  - provider/aws
# License of this project
license: "APACHE2"
# Badges to display
badges:
  - name: Latest Release
    image: https://img.shields.io/github/release/cloudposse-terraform-components/aws-lakeformation.svg?style=for-the-badge
    url: https://github.com/cloudposse-terraform-components/aws-lakeformation/releases/latest
  - name: Slack Community
    image: https://slack.cloudposse.com/for-the-badge.svg
    url: https://slack.cloudposse.com
related:
  - name: "Cloud Posse Terraform Modules"
    description: Our collection of reusable Terraform modules used by our reference architectures.
    url: "https://docs.cloudposse.com/modules/"
  - name: "Atmos"
    description: "Atmos is like docker-compose but for your infrastructure"
    url: "https://atmos.tools"
contributors: [] # If included generates contribs
